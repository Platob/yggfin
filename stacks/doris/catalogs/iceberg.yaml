# rekep.records.doris.DorisCatalog; the file stem defaults `name`.
# An Iceberg lakehouse catalog; properties override the REST defaults.
type: iceberg
properties:
  uri: "{{ env.get('ICEBERG_REST_URI', 'http://rest:8181') }}"
  warehouse: "{{ env.get('ICEBERG_WAREHOUSE', 's3://warehouse/') }}"
