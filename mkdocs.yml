site_name: rekep
site_description: Trading logs, Arrow-native fields, and Iceberg datasets
site_url: https://platob.github.io/yggfin/
repo_url: https://github.com/Platob/yggfin
repo_name: Platob/yggfin
docs_dir: docs

theme:
  name: material
  features:
    - content.code.copy
    - content.tabs.link
    - navigation.sections
    - navigation.top
    - toc.follow
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: teal
      toggle:
        icon: material/weather-night
        name: Dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: teal
      toggle:
        icon: material/weather-sunny
        name: Light mode

nav:
  - Home: index.md
  - Architecture:
      - Design rules: design.md
      - Row ids: ids.md
      - Schema contracts: contracts.md
  - Guides:
      - Types: types.md
      - Logs: logs.md
      - FIX: fix.md
      - Iceberg: iceberg.md
  - Benchmarks: benchmarks.md

# Every page carries its own measurements, so the links into them are anchors
# rather than page names -- and an anchor mkdocs cannot find is only logged at
# INFO by default, which is a broken link that ships. Raised to a warning, so
# --strict fails on it in CI like any other broken link.
validation:
  links:
    anchors: warn

markdown_extensions:
  - admonition
  - attr_list
  - def_list
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
      slugify: !!python/object/apply:pymdownx.slugs.slugify {kwds: {case: lower}}
