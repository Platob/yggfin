[
  {
    "name": "currunix",
    "dtype": {
      "type": "datetime64",
      "unit": "nanosecond",
      "timezone": "UTC"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65003",
      "FIX:transient": "false",
      "description": "When the message happened: the settled instant, UTC.",
      "display": "CurrUnix"
    }
  },
  {
    "name": "msgctxid",
    "dtype": {
      "type": "string"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65008",
      "description": "The message context a bridge handled the message in, as its own log names it.",
      "display": "MsgCtxId"
    }
  },
  {
    "name": "msgpluginid",
    "dtype": {
      "type": "string"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65009",
      "description": "The plugin that logged the line inside a bridge, as the bridge names it: the row's own msgpluginid column, never derived.",
      "display": "MsgPluginId"
    }
  },
  {
    "name": "currhashcode",
    "dtype": {
      "type": "uint64"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65017",
      "FIX:transient": "false",
      "description": "The XXH3-64 of what the event states and the named FIX content behind it.",
      "display": "CurrHashCode"
    }
  },
  {
    "name": "crosshashcode",
    "dtype": {
      "type": "uint64"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65018",
      "FIX:transient": "false",
      "description": "The XXH3-64 of the cross code; zero where the message names none.",
      "display": "CrossHashCode"
    }
  },
  {
    "name": "prevunix",
    "dtype": {
      "type": "datetime64",
      "unit": "nanosecond",
      "timezone": "UTC"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65021",
      "FIX:transient": "false",
      "description": "When the message this one follows happened, where it follows one.",
      "display": "PrevUnix"
    }
  },
  {
    "name": "prevuuid",
    "dtype": {
      "type": "uuid"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65022",
      "FIX:transient": "false",
      "description": "The identity of the message this one follows, where it follows one.",
      "display": "PrevUuid"
    }
  },
  {
    "name": "creaunix",
    "dtype": {
      "type": "datetime64",
      "unit": "nanosecond",
      "timezone": "UTC"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65023",
      "FIX:transient": "false",
      "description": "When the message was created: what it states, else when the original was sent, else when it happened; the earliest its chain knows once followed.",
      "display": "CreaUnix"
    }
  },
  {
    "name": "snapunix",
    "dtype": {
      "type": "datetime64",
      "unit": "nanosecond",
      "timezone": "UTC"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65025",
      "FIX:transient": "false",
      "description": "The grid instant a walk read this message as the snapshot of; empty on every row no snapshot was taken of.",
      "display": "SnapUnix"
    }
  },
  {
    "name": "sourceurl",
    "dtype": {
      "type": "url"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65026",
      "FIX:transient": "false",
      "description": "The object this message's line was read from.",
      "display": "SourceUrl"
    }
  },
  {
    "name": "nofixentries",
    "dtype": {
      "type": "int32"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65027",
      "FIX:transient": "false",
      "description": "How many pairs the message carried, in arrival order.",
      "display": "NoFixEntries"
    }
  },
  {
    "name": "msgsessionid",
    "dtype": {
      "type": "string"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65032",
      "description": "The session instance a bridge handled a line on, as its own row header brackets it - never what the message states about itself.",
      "display": "MsgSessionId"
    }
  },
  {
    "name": "curruuid",
    "dtype": {
      "type": "uuid"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65039",
      "FIX:transient": "false",
      "description": "The message's identity: the UUIDv7 its instant and its code derive.",
      "display": "CurrUuid"
    }
  },
  {
    "name": "crossuuid",
    "dtype": {
      "type": "uuid"
    },
    "nullable": false,
    "metadata": {
      "FIX:tag": "65040",
      "FIX:transient": "false",
      "description": "The identity every message of one lifecycle shares, derived from the identifier they share; the message's own where it names none.",
      "display": "CrossUuid"
    }
  },
  {
    "name": "parentuuids",
    "dtype": {
      "type": "list",
      "field": {
        "name": "parentuuid",
        "dtype": {
          "type": "uuid"
        },
        "nullable": false,
        "metadata": {}
      }
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65041",
      "FIX:transient": "false",
      "description": "The identities of the messages this one descends from, in the order it states them.",
      "display": "ParentUuids"
    }
  },
  {
    "name": "seqnum",
    "dtype": {
      "type": "uint64"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65042",
      "FIX:transient": "false",
      "description": "The message's place in its chain: how many came before it.",
      "display": "SeqNum"
    }
  },
  {
    "name": "crosscode",
    "dtype": {
      "type": "string"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65048",
      "FIX:transient": "false",
      "description": "The identifier every message of one lifecycle shares: OrderID, else ClOrdID, OrigClOrdID, QuoteID, QuoteReqID or MDReqID, the first stated.",
      "display": "CrossCode"
    }
  },
  {
    "name": "srcuuids",
    "dtype": {
      "type": "list",
      "field": {
        "name": "srcuuid",
        "dtype": {
          "type": "uuid"
        },
        "nullable": false,
        "metadata": {}
      }
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65051",
      "FIX:transient": "false",
      "description": "The identities of the elements this message was read from: the text line it was parsed out of, and none for one parsed from raw bytes. Provenance, never lineage: no walk moves it.",
      "display": "SrcUuids"
    }
  },
  {
    "name": "state",
    "dtype": {
      "type": "state"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65052",
      "FIX:transient": "false",
      "description": "The state the message reached, ranked so the column sorts by lifecycle: OrdStatus, else ExecType, 00UNKNOWN where neither states one; the furthest its chain knows once followed.",
      "display": "State"
    }
  },
  {
    "name": "expirunix",
    "dtype": {
      "type": "datetime64",
      "unit": "nanosecond",
      "timezone": "UTC"
    },
    "nullable": true,
    "metadata": {
      "FIX:tag": "65053",
      "FIX:transient": "false",
      "description": "When the message stops being good: ExpireTime, else ValidUntilTime, ExpireDate or MaturityDate; the latest its chain knows once followed.",
      "display": "ExpirUnix"
    }
  }
]
