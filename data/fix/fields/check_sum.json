{
 "name": "CheckSum",
 "tag": 10,
 "versions": {
  "5.0.SP2": {
   "type": "String",
   "description": "Three byte, simple checksum (see FIXT.1.1: \"CheckSum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)",
   "components": [
    "StandardTrailer"
   ]
  },
  "5.0.SP1": {
   "type": "String",
   "description": "Three byte, simple checksum (see FIXT.1.1: \"CheckSum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)",
   "components": [
    "StandardTrailer"
   ]
  },
  "5.0": {
   "type": "String",
   "description": "Three byte, simple checksum (see FIXT.1.1: \"CheckSum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "4.4": {
   "type": "String",
   "description": "Three bytes, simple checksum (see Volume 2: \"Checksum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "4.3": {
   "type": "String",
   "description": "Three byte, simple checksum (see Volume 2: \"Checksum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "4.2": {
   "type": "String",
   "description": "Three byte, simple checksum (see Appendix B: CheckSum Calculation for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing <SOH>, as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "4.1": {
   "type": "char",
   "description": "Three byte, simple checksum (see Appendix B: \"CheckSum Calculation\" for description). ALWAYS LAST FIELD IN RECORD; i.e. serves, with the trailing <SOH>, as the end-of-record delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "4.0": {
   "type": "char",
   "description": "Three byte, simple checksum (see Appendix B: \"CheckSum Calculation\" for description). ALWAYS LAST FIELD IN RECORD; i.e. serves, with the trailing <SOH>, as the end-of-record delimiter. Always defined as three characters. (Always unencrypted)"
  },
  "FIXT1.1": {
   "type": "String",
   "description": "Three byte, simple checksum (see Volume 2: \"Checksum Calculation\" for description). ALWAYS LAST FIELD IN MESSAGE; i.e. serves, with the trailing , as the end-of-message delimiter. Always defined as three characters. (Always unencrypted)",
   "components": [
    "StandardTrailer"
   ]
  }
 }
}